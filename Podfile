# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

target 'BackpackConfigurator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BackpackConfigurator
    pod 'SwiftSocket'
    pod 'FGRoute'
    pod 'SwiftyJSON', '~> 4.0'
    pod 'KeychainAccess'

  target 'BackpackConfiguratorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BackpackConfiguratorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
